# Refactored Dispatch App

Run with:

```bash
pip install -r requirements.txt
streamlit run app.py
```

This repository was modularized from `Full Dispatch (2).py` while preserving the original business logic and Streamlit app behavior.
